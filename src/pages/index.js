import React from 'react';
import Typography from '@material-ui/core/Typography';
import Layout from '../components/layout';

export default () => (
  <Layout>
    <Typography component='h1'>Amazing Pandas Eating Things</Typography>
  </Layout>
);
